# アニマルピース ルール仕様書 (v2.0)

このファイルは、各ピース（動物）のスコア計算ルールおよびマージ（進化）時の仕様を定義するマスタードキュメントです。

## 1. 基本システム（共通ルール）

### ① 配置スコア
ピースが盤面に配置された瞬間に、そのレベルに応じた「基本スコア」が加算されます。
- **Lv1**: 10点
- **Lv2**: 20点
- **Lv3**: 30点

※手札から置いた時だけでなく、マージ（進化）によって新しいピースが生成された時も、この配置スコアは加算されます。

### ② マージ（進化）スコア
同じ動物かつ同じレベルのピースが「4つ以上」上下左右に繋がるとマージし、最後に置いた場所に1つ上のレベルのピースが生成されます。
このとき、直前まで繋がっていたピースの数とレベルに応じてスコアが加算されます。
- **基本計算式**: `(繋がっていたピース数) × (元のレベル) × 100点`
  - *例: Lv1が4つくっついてLv2になった場合 → `4 × 1 × 100 = 400点`（さらにLv2配置の20点が加算され、合計420点）*

### ③ お邪魔ルール：シロクマの威圧
ピースを「配置」または「マージ」した場所の**上下左右のいずれか**に「シロクマ」が1体でもいる場合、シロクマ以外の動物はその時のスコア（配置スコア・マージスコア）がすべて **0点** になります。
※シロクマ自身を配置・マージした場合は、このルールを受けず通常通り点数が入ります。

---

## 2. 動物ごとの特殊シナジー（マージ時）

各動物は、マージ（進化）したマスを基準として、隣接（上下左右）している特定の動物によってスコア倍率が変動します。

### アザラシ（Seal）
- **特性**: リスと仲良し
- **特殊ルール**: マージする際、合体先の周囲に「リス」がいると、リス1体につきマージスコアの倍率が `0.2倍`（+20%）加算されます。
  - *計算式: `マージスコア × (1.0 + 隣接するリスの数 × 0.2)`*
  - *例: 1匹隣接なら 1.2倍、2匹なら 1.4倍*

### ペンギン（Penguin）
- **特性**: 群れるほど強力 ＆ リスのサポート
- **特殊ルール**: 
  1. マージする際、合体に関わったピース数が多いほどボーナス倍率が乗ります（4個より多い分、1つにつき +0.5倍）。
     - *計算式: `マージスコア × (1.0 + (ピース数 - 4) × 0.5)`*
     - *例: 5個なら 1.5倍、6個なら 2.0倍*
  2. アザラシと同様に、合体先の周囲に「リス」がいると、リス1体につきさらに倍率が `0.2倍` 加算されます。
     - *計算式: `(上記計算後のスコア) × (1.0 + 隣接するリスの数 × 0.2)`*

### シロクマ（Polar Bear）
- **特性**: 他を圧倒するお邪魔キャラ
- **特殊ルール**: 自身のマージ時に特殊なシナジーボーナスはありませんが、盤面に存在するだけで他の動物（アザラシ・ペンギン・リス）のスコア発生を無効化（0点）にします。

### リス（Squirrel）
- **特性**: 優秀なサポーター
- **特殊ルール**: 自身のマージ時に特殊なシナジーボーナスはありません。しかし、盤面に配置しておくことで、隣接してマージした「アザラシ」と「ペンギン」のスコアを底上げする重要なサポート役となります。
